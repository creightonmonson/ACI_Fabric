{
    "ospfExtP": {
      "attributes": {
        "status": "{{(state=='present')|ternary('created,modified','deleted')}}",
        "areaId": "{{l3out.name[0].ospf_area_id|default('1')}}",
        "areaType": "{{l3out.name[0].ospf_area_type|default('nssa')}}",
        "areaCost": "{{l3out.name[0].ospf_area_cost|default('1')}}"
      }
    }
}